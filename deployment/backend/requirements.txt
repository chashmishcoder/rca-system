# Phase 5 API Requirements
# Install with: pip install -r requirements.txt

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# CORS support
python-multipart==0.0.6

# LangChain and LangGraph for AI agents
# langchain-groq requires langchain-core>=0.3.x — drop all 0.2.x upper bounds
langchain-groq>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain>=0.3.0
langgraph>=0.2.0

# Neo4j for Knowledge Graph
neo4j==5.14.1

# ML and Data Processing
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
# Use CPU-only TF build on Render (no GPU available) — avoids CUDA init errors.
# TF 2.16+ depends on standalone keras>=3.0 (required for .keras model format).
tensorflow-cpu>=2.16.0
keras>=3.0.0

# Additional utilities
requests>=2.31.0
python-dotenv>=1.0.0
